{% extends '::layout/layoutAdmin.html.twig' %}

{% block body -%}
    <h1>Client Utilisateurs</h1>

    <form action="{{ path('adminUsers_update', { 'id' : entity.id }) }}" method="POST" {{ form_enctype(edit_form) }}>
        {{ form(edit_form) }}
    </form>

    <ul class="record_actions">
        <li>
            <a href="{{ path('adminUsers') }}">
                Back to the list
            </a>
        </li>
        <li>{{ form(delete_form) }}</li>
    </ul>
{% endblock %}