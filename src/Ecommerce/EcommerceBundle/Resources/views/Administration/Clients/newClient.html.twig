{% extends '::layout/layoutAdmin.html.twig' %}

{% block body -%}
    <h1>Ajouter Photo Client</h1>

    <form action="{{ path('adminClient_create') }}" method="POST" {{ form_enctype(form) }}>
        {{ form(form) }}
    </form>

    <ul class="record_actions">
        <li>
            <a href="{{ path('adminClient') }}">
                Back to the list
            </a>
        </li>
    </ul>
{% endblock %}